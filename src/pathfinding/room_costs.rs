use screeps::{Position, RoomName};
use screeps_utils::sparse_cost_matrix::SparseCostMatrix;

pub fn economy_room_costs(room_name: &RoomName) -> SparseCostMatrix {
    let mut costs = SparseCostMatrix::new();

    costs
}